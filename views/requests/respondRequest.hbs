<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Respond to Trade Requests</title>
  <link href="/css/styles.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">

  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="w-full">
  {{> upperPart }}

  <div class="container mx-auto my-8 px-4">
    <h1 class="text-3xl font-bold mb-5 text-center">Respond to Trade Requests</h1>

    {{#if respondRequests.length}}
    <div class="flex flex-wrap justify-center">
      {{#each respondRequests}}
      <div class="card !static bg-cyan-700 text-white shadow-xl w-full sm:w-4/5 lg:w-3/5 mb-6 p-4 lg:p-6 rounded-lg">
        <div class="card-body">
          <h2 class="text-2xl font-extrabold mb-4 text-center lg:text-left">
            <i class="fas fa-exchange-alt mr-2"></i> Trade Request from {{this.requester.username}}
          </h2>

          <h3 class="text-lg font-bold mb-2">Books Offered to You:</h3>
          <ul class="list-none mb-4">
            {{#each this.offeredBooks}}
            <li class="mb-1 text-left">
              <i class="fas fa-book mr-2"></i><span class="font-bold">{{this.title}}</span> by {{this.giver}}
            </li>
            {{/each}}
          </ul>

          <h3 class="text-lg font-bold mb-2">Books Requested from You:</h3>
          <ul class="list-none mb-4">
            {{#each this.requestedBooks}}
            <li class="mb-1 text-left">
              <i class="fas fa-book mr-2"></i><span class="font-bold">{{this.title}}</span> by {{this.author}}
            </li>
            {{/each}}
          </ul>

          <div class="flex justify-center mt-4">
            <form action="/requests/accept" method="POST" class="mx-2">
              <input type="hidden" name="tradeRequestId" value="{{this.id}}">
              <button type="submit" class="btn bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded-lg">
                Accept
              </button>
            </form>

            <form action="/requests/reject" method="POST" class="mx-2">
              <input type="hidden" name="tradeRequestId" value="{{this.id}}">
              <button type="submit" class="btn bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded-lg">
                Reject
              </button>
            </form>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
    {{else}}
    <div class="flex flex-wrap justify-center">
      <div class="card !static bg-gray-100 shadow-xl w-full sm:w-4/5 lg:w-3/5 mb-6 p-4 lg:p-6 rounded-lg text-center">
        <div class="card-body">
          <h3 class="text-2xl font-extrabold mb-4 text-gray-700">
            <i class="fas fa-exclamation-circle mr-2"></i> No Trade Requests to Respond To
          </h3>
          <p class="text-lg text-gray-500">
            There are currently no trade requests available for you to respond to. Please check back later.
          </p>
        </div>
      </div>
    </div>
    {{/if}}

    {{#if isAuthenticated}}
    <div class="user-info text-center mt-8">
      <p>Logged in as: {{user.username}}</p>
      <a href="/logout" class="text-blue-500 hover:underline">Logout</a>
    </div>
    {{/if}}
  </div>

  {{> belowPart }}

</body>

</html>
